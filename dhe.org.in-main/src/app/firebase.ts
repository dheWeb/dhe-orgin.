/**
 * @deprecated Import from `@/services/firebase/client` in new code.
 * Kept for backward-compatible imports across the codebase.
 */
export { app, auth, db, storage } from "@/services/firebase/client";
